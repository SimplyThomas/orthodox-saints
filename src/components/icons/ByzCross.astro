---
import { byzCross } from "../../lib/icons";

interface Props {
  size?: number;
  color?: string;
  stroke?: number;
}
const { size = 22, color = "currentColor", stroke = 2.2 } = Astro.props;
---

<Fragment set:html={byzCross(size, color, stroke)} />
