---
/* "Giving Icons as Gifts" — a practical guide to which icons are given for
   which occasion, why, and where they end up in the home.

   All content lives in lib/icon-gifts.ts; this file is presentation only. Every
   named icon subject resolves through resolveGiftRef(), so a suggestion links
   to a live record when one exists and renders as plain text when it doesn't —
   see the future-proofing note at the top of that file.

   The hero composes a real icon corner out of database portraits. Most of those
   are vendor-permission images (§9), so the hero carries the grant's required
   attribution and links each icon back to its own vendor page. If the vendor is
   ever revoked, build.py drops `image` from the records and the corner falls
   back to monogram avatars on its own — see heroIcons below. */

import EyebrowRule from "./EyebrowRule.astro";
import DomeMark from "./icons/DomeMark.astro";
import { saintAvatar } from "../lib/icons";
import { withBase } from "../lib/format";
import {
  IG_HERO,
  IG_INTRO,
  IG_OCCASIONS,
  IG_VOCATIONS,
  IG_TIERS,
  IG_RECIPIENTS,
  IG_TIPS,
  IG_RELATED,
  resolveGiftRef,
  type GiftIcon,
  type GiftTier,
} from "../lib/icon-gifts";

/* ---------- small line glyphs (gold, 24×24 stroke) ---------- */
const GLYPH: Record<string, string> = {
  infant:
    '<circle cx="12" cy="6.8" r="3.1"/><path d="M6.4 20.5c0-3.6 2.5-6.6 5.6-6.6s5.6 3 5.6 6.6"/><path d="M9.2 17.8h5.6"/>',
  font: '<path d="M12 1.8v4.4M10 3.9h4"/><path d="M5 8.4h14"/><path d="M6.6 8.4v5.4a5.4 5.4 0 0 0 10.8 0V8.4"/><path d="M12 19.2v2.6M9 21.8h6"/>',
  candle:
    '<rect x="9.4" y="8.6" width="5.2" height="12.8" rx="1"/><path d="M12 8.6c0-2 1.9-2.5 1.9-4.3C13.9 3 12 1.8 12 1.8s-1.9 1.2-1.9 2.5C10.1 6.1 12 6.6 12 8.6Z"/>',
  crown:
    '<path d="M4.4 18.6h15.2"/><path d="M4.4 18.6 2.8 8.4l4.9 3.7L12 5.2l4.3 6.9 4.9-3.7-1.6 10.2"/>',
  house:
    '<path d="M3.6 11.4 12 3.8l8.4 7.6"/><path d="M5.8 10.2v10h12.4v-10"/><path d="M10.2 20.2v-5.1h3.6v5.1"/>',
  scroll:
    '<path d="M6.2 2.8h8.6l3.4 3.4v15H6.2z"/><path d="M14.8 2.8v3.4h3.4"/><path d="M9 9.6h6.4M9 13.2h6.4M9 16.8h4"/>',
  tools:
    '<path d="M3.4 20.6 11 13"/><path d="M10 8.6 13.8 4.8l1.6 1.6L19.2 2.6l2.2 2.2-3.8 3.8 1.6 1.6-3.8 3.8-5.4-5.4Z"/>',
  star: '<path d="M12 1.8v20.4M1.8 12h20.4M4.8 4.8l14.4 14.4M19.2 4.8 4.8 19.2"/>',
  dove: '<path d="M2.6 13.4c4.4-.8 7-3.8 9.2-7.4 1.2 3.4 3.4 5.4 6.6 5.4-.8 4.6-4.6 8.2-9.2 8.2-3.2 0-5.6-2.4-6.6-6.2Z"/><path d="M11.8 6 15 2.8"/>',
  vial: '<path d="M9 2.4h6"/><path d="M10.2 2.4v6.4L6.6 16a3.6 3.6 0 0 0 3.1 5.6h4.6a3.6 3.6 0 0 0 3.1-5.6l-3.6-7.2V2.4"/><path d="M8.2 14.6h7.6"/>',
  chalice:
    '<path d="M6.8 2.8h10.4"/><path d="M7.8 2.8v5.4a4.2 4.2 0 0 0 8.4 0V2.8"/><path d="M12 12.4v6.6"/><path d="M8.4 21.4h7.2"/>',
};

function glyph(kind: string, size = 26, color = "var(--gold-deep)") {
  return `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="${color}" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${GLYPH[kind] ?? GLYPH.candle}</svg>`;
}

/* ---------- the hero icon corner ---------- */
/* Resolve first: a revoked vendor (§9) strips `image` from every record, so the
   corner must never assume a portrait is there. Icons with a portrait render it;
   the rest fall back to the monogram avatar; if the whole corner comes back
   empty we drop to the site's DomeMark ornament rather than show a bare shelf. */
const heroIcons = IG_HERO.map((h) => {
  const r = resolveGiftRef(h.ref);
  return { ...h, r, portrait: r?.image ? withBase(r.image) : null };
}).filter((h) => h.r);
const heroHasPortraits = heroIcons.some((h) => h.portrait);

/* The grant's condition: each image links back to its own icon page. */
const heroCredits = heroIcons.filter(
  (h) => h.r?.imagePermission && h.r?.imageSource,
);
const heroVendor = heroCredits[0]?.r?.imageVendor ?? "";
const heroAttribution = heroCredits[0]?.r?.imageAttribution ?? "";

const TIER_LABEL: Record<GiftTier, string> = {
  traditional: "Most traditional",
  regional: "Regional custom",
  modern: "Modern suggestion",
};

/* A suggestion renders as a link when its subject is a record, plain text when
   it isn't (Christ Pantocrator as an icon *subject* is not a database row). */
function iconHref(i: GiftIcon): string | null {
  return resolveGiftRef(i.ref)?.href ?? null;
}
---

<div class="igp">
  {/* ============================ HERO ============================ */}
  <section class="sr-hero ig-hero">
    {
      heroHasPortraits ? (
        <div class="ig-corner">
          <div class="ig-lamp" aria-hidden="true">
            <span class="ig-lamp-chain" />
            <span class="ig-lamp-flame" />
            <span class="ig-lamp-bowl" />
          </div>
          <ul class="ig-corner-row">
            {heroIcons.map((h) => (
              <li class={`ig-corner-item${h.center ? " is-center" : ""}`}>
                <a class="ig-corner-link" href={h.r!.href}>
                  <span class="ig-frame">
                    {h.portrait ? (
                      <img
                        class="ig-frame-img"
                        src={h.portrait}
                        alt={`Icon of ${h.label}`}
                        loading="eager"
                        decoding="async"
                      />
                    ) : (
                      <Fragment
                        set:html={saintAvatar({ name: h.label }, 92, 116)}
                      />
                    )}
                  </span>
                </a>
              </li>
            ))}
          </ul>
          <span class="ig-shelf" aria-hidden="true" />
        </div>
      ) : (
        <div class="dome-bg" aria-hidden="true">
          <DomeMark w={460} color="var(--byz)" />
        </div>
      )
    }

    <div class="sr-hero-inner">
      <EyebrowRule label="The Orthodox Home" />
      <h1 class="sr-title">Giving Icons as Gifts</h1>
      <p class="sr-lede">
        Throughout the Orthodox Christian life, icons are given to mark the
        milestones. Whether you are celebrating a birth, a baptism, a marriage,
        or another turning point, an icon is a lasting reminder that Christ and
        His saints accompany us through every stage of life.
      </p>
      <div class="ig-hero-cta">
        <a class="btn btn--gold" href="#occasions">Browse by occasion</a>
        <a class="btn btn--ghost" href="#who">Who are you buying for?</a>
      </div>
    </div>

    {
      /* The grant's condition is that each image links back to its own icon
         page, so every icon above is named here and linked individually — the
         vendor is credited once rather than repeated four times. */
      heroCredits.length > 0 && (
        <p class="ig-credit">
          {heroAttribution.replace(/^Icon /, "Icons ")} Original icons on{" "}
          {heroVendor}:{" "}
          {heroCredits.map((h, n) => (
            <>
              {n > 0 && <span aria-hidden="true"> · </span>}
              <a
                href={h.r!.imageSource}
                target="_blank"
                rel="noopener noreferrer"
              >
                {h.label}
              </a>
            </>
          ))}
        </p>
      )
    }
  </section>

  {/* ============================ INTRO ============================ */}
  <section class="ig-sec ig-intro-sec">
    <div class="ig-wrap ig-wrap--narrow">
      <div class="ig-sec-head">
        <div class="eyebrow">Why we give them</div>
        <h2>A window, not an ornament</h2>
      </div>
      {IG_INTRO.map((p) => <p class="ig-prose">{p}</p>)}
    </div>
  </section>

  {/* ========================== OCCASIONS ========================== */}
  <section class="ig-sec ig-sec--ivory" id="occasions">
    <div class="ig-wrap">
      <div class="ig-sec-head">
        <div class="eyebrow">Occasions</div>
        <h2>Which icon, for which moment</h2>
        <p>
          Every card opens onto the fuller answer &mdash; why the icon is given,
          when it is presented, and where it is kept afterward.
        </p>
      </div>

      <div class="ig-cards">
        {
          IG_OCCASIONS.map((o) => (
            <article
              class={`ig-card${o.large ? " is-large" : ""}`}
              id={`occasion-${o.id}`}
            >
              <div class="ig-card-head">
                <span class="ig-card-glyph" set:html={glyph(o.glyph, 26)} />
                <div class="eyebrow">{o.kicker}</div>
              </div>
              <h3 class="ig-card-title">{o.title}</h3>
              <p class="ig-card-blurb">{o.blurb}</p>

              <details class="ig-more">
                <summary class="ig-more-sum">
                  <span class="ig-more-rule" aria-hidden="true" />
                  <span class="ig-more-state">
                    <span class="ig-more-show">Learn more</span>
                    <span class="ig-more-hide">Close</span>
                  </span>
                  <svg
                    class="ig-chev"
                    width="15"
                    height="15"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="var(--celest)"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    aria-hidden="true"
                  >
                    <path d="m6 9 6 6 6-6" />
                  </svg>
                </summary>

                <div class="ig-more-body">
                  <p class="ig-prose">{o.intro}</p>

                  {o.icons.length > 0 && (
                    <ul class="ig-icons">
                      {o.icons.map((i) => {
                        const href = iconHref(i);
                        return (
                          <li class="ig-icon">
                            <div class="ig-icon-head">
                              {href ? (
                                <a class="ig-icon-name" href={href}>
                                  {i.name}
                                </a>
                              ) : (
                                <span class="ig-icon-name is-plain">
                                  {i.name}
                                </span>
                              )}
                              <span class={`ig-tier is-${i.tier}`}>
                                {TIER_LABEL[i.tier]}
                              </span>
                            </div>
                            <p class="ig-icon-why">{i.why}</p>
                            {i.where && <p class="ig-icon-where">{i.where}</p>}
                          </li>
                        );
                      })}
                    </ul>
                  )}

                  {o.id === "vocation" && (
                    <div class="ig-vocs">
                      {IG_VOCATIONS.map((v) => {
                        const r = resolveGiftRef(v.ref);
                        return (
                          <a class="ig-voc" href={r!.href}>
                            <span class="ig-voc-label">{v.label}</span>
                            <span class="ig-voc-note">{v.note}</span>
                          </a>
                        );
                      })}
                    </div>
                  )}

                  {o.note && (
                    <aside class="ig-note">
                      <div class="corner tl" aria-hidden="true" />
                      <div class="corner tr" aria-hidden="true" />
                      <div class="corner bl" aria-hidden="true" />
                      <div class="corner br" aria-hidden="true" />
                      <h4 class="ig-note-title">{o.note.title}</h4>
                      <p class="ig-note-body">{o.note.body}</p>
                    </aside>
                  )}

                  <dl class="ig-facts">
                    <div class="ig-fact">
                      <dt>Why it is given</dt>
                      <dd>{o.why}</dd>
                    </div>
                    <div class="ig-fact">
                      <dt>When it is presented</dt>
                      <dd>{o.when}</dd>
                    </div>
                    <div class="ig-fact">
                      <dt>Where it is kept</dt>
                      <dd>{o.where}</dd>
                    </div>
                  </dl>
                </div>
              </details>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  {/* ============================ TIERS ============================ */}
  <section class="ig-sec ig-sec--deep" id="tiers">
    <div class="ig-wrap">
      <div class="ig-sec-head is-light">
        <div class="eyebrow on-blue">Knowing the difference</div>
        <h2>Tradition, custom, and good ideas</h2>
        <p>
          Not everything an Orthodox Christian does is a tradition of the
          Church, and not every tradition is kept everywhere. Telling them apart
          is a courtesy to inquirers &mdash; and the surest way to keep the real
          ones intact.
        </p>
      </div>

      <div class="ig-tiers">
        {
          IG_TIERS.map((t) => (
            <article class={`ig-tier-card is-${t.tier}`}>
              <div class="ig-tier-star" aria-hidden="true">
                <span class="dm" />
              </div>
              <div class="ig-tier-label">{t.label}</div>
              <p class="ig-tier-lede">{t.lede}</p>
              <p class="ig-tier-body">{t.body}</p>
              <ul class="ig-tier-list">
                {t.examples.map((e) => (
                  <li>{e}</li>
                ))}
              </ul>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  {/* ====================== WHO ARE YOU BUYING FOR ====================== */}
  <section class="ig-sec ig-sec--ivory" id="who">
    <div class="ig-wrap">
      <div class="ig-sec-head">
        <div class="eyebrow">A place to start</div>
        <h2>Who are you buying for?</h2>
        <p>
          Choose the person and see what is usually given &mdash; and what is
          better left to their godparent.
        </p>
      </div>

      <div class="ig-who" data-who>
        <div class="ig-who-tabs" role="list">
          {
            IG_RECIPIENTS.map((r, n) => (
              <button
                type="button"
                class="ig-who-tab"
                role="listitem"
                data-who-tab={r.id}
                aria-expanded={n === 0 ? "true" : "false"}
                aria-controls={`who-${r.id}`}
              >
                <span class="ig-who-glyph" set:html={glyph(r.glyph, 18)} />
                {r.label}
              </button>
            ))
          }
        </div>

        {
          /* Every panel renders visible; the island hides all but one. Without
            JS this stays a complete, readable list rather than an empty box. */
        }
        <div class="ig-who-panels">
          {
            IG_RECIPIENTS.map((r) => (
              <div
                class="ig-who-panel"
                id={`who-${r.id}`}
                data-who-panel={r.id}
              >
                <h3 class="ig-who-title">{r.label}</h3>
                <p class="ig-who-intro">{r.intro}</p>
                <ul class="ig-icons ig-icons--tight">
                  {r.icons.map((i) => {
                    const href = iconHref(i);
                    return (
                      <li class="ig-icon">
                        <div class="ig-icon-head">
                          {href ? (
                            <a class="ig-icon-name" href={href}>
                              {i.name}
                            </a>
                          ) : (
                            <span class="ig-icon-name is-plain">{i.name}</span>
                          )}
                          <span class={`ig-tier is-${i.tier}`}>
                            {TIER_LABEL[i.tier]}
                          </span>
                        </div>
                        <p class="ig-icon-why">{i.why}</p>
                        {i.where && <p class="ig-icon-where">{i.where}</p>}
                      </li>
                    );
                  })}
                </ul>
              </div>
            ))
          }
        </div>
      </div>
    </div>
  </section>

  {/* ============================= TIPS ============================= */}
  <section class="ig-sec" id="tips">
    <div class="ig-wrap">
      <div class="ig-sec-head">
        <div class="eyebrow">Before you buy</div>
        <h2>Choosing well</h2>
      </div>
      <div class="ig-tips">
        {
          IG_TIPS.map((t) => (
            <article class="ig-tip">
              <span class="ig-tip-dm" aria-hidden="true" />
              <h3 class="ig-tip-title">{t.title}</h3>
              <p class="ig-tip-body">{t.body}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  {/* =========================== RELATED =========================== */}
  <section class="ig-sec ig-sec--ivory" id="related">
    <div class="ig-wrap">
      <div class="ig-sec-head">
        <div class="eyebrow">Keep exploring</div>
        <h2>Where to go next</h2>
      </div>
      <div class="ig-related">
        {
          IG_RELATED.map((r) => (
            <a class="ig-rel" href={r.href}>
              <span class="ig-rel-glyph" set:html={glyph(r.glyph, 24)} />
              <span class="ig-rel-text">
                <span class="ig-rel-label">{r.label}</span>
                <span class="ig-rel-blurb">{r.blurb}</span>
              </span>
            </a>
          ))
        }
      </div>
    </div>
  </section>
</div>

<style>
  /* ---------------- layout ---------------- */
  .ig-wrap {
    max-width: 1080px;
    margin: 0 auto;
    padding: 0 clamp(16px, 4vw, 40px);
  }
  .ig-wrap--narrow {
    max-width: 720px;
  }
  .ig-sec {
    padding: clamp(48px, 7vw, 76px) 0;
  }
  .ig-sec--ivory {
    background: var(--ivory-2);
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }
  .ig-sec--deep {
    background: var(--byz-deep);
  }

  .ig-sec-head {
    text-align: center;
    max-width: 680px;
    margin: 0 auto clamp(28px, 4vw, 44px);
  }
  .ig-sec-head h2 {
    font-family: var(--serif);
    font-weight: 600;
    font-size: clamp(30px, 4.4vw, 42px);
    color: var(--byz-deep);
    margin: 10px 0 0;
  }
  .ig-sec-head p {
    font-size: 17px;
    line-height: 1.6;
    color: var(--ink-soft);
    margin: 14px 0 0;
    text-wrap: balance;
  }
  .ig-sec-head.is-light h2 {
    color: var(--ivory);
  }
  .ig-sec-head.is-light p {
    color: var(--celest-soft);
  }

  .ig-prose {
    font-size: 17px;
    line-height: 1.72;
    color: var(--ink);
    margin: 0 0 18px;
  }
  .ig-prose:last-child {
    margin-bottom: 0;
  }

  /* ---------------- hero icon corner ---------------- */
  .ig-hero {
    padding-bottom: 34px;
  }
  .ig-corner {
    position: relative;
    width: min(560px, 92vw);
    margin: 0 auto 26px;
    padding-top: 34px;
  }
  .ig-corner-row {
    display: flex;
    align-items: flex-end;
    justify-content: center;
    gap: clamp(8px, 2vw, 16px);
    margin: 0;
    padding: 0;
    list-style: none;
  }
  .ig-corner-item {
    flex: 0 0 auto;
  }
  /* The link is only as tall as its frame, so the icons bottom-align and the
     shelf sits directly beneath them. The icons are deliberately unlabelled —
     an icon corner has no placards, and the credit line below names and links
     all four in order (each icon also carries alt text and its own link). */
  .ig-corner-link {
    display: block;
    text-decoration: none;
    color: inherit;
  }
  .ig-frame {
    display: block;
    padding: 3px;
    background: linear-gradient(160deg, var(--gold-soft), var(--gold-deep));
    border-radius: 4px;
    box-shadow: var(--shadow-card);
    transition:
      transform 0.15s,
      box-shadow 0.15s;
  }
  .ig-frame-img {
    display: block;
    width: 62px;
    height: 78px;
    object-fit: cover;
    object-position: center top;
    border-radius: 2px;
    background: #16304c;
  }
  .is-center .ig-frame-img {
    width: 84px;
    height: 106px;
  }
  .ig-corner-link:hover .ig-frame {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  .ig-shelf {
    display: block;
    height: 2px;
    margin: 5px auto 0;
    width: min(460px, 84vw);
    background: linear-gradient(
      90deg,
      transparent,
      var(--gold-deep) 12%,
      var(--gold-deep) 88%,
      transparent
    );
    border-radius: 2px;
  }
  /* the vigil lamp kept burning before the icons */
  .ig-lamp {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .ig-lamp-chain {
    width: 1px;
    height: 16px;
    background: var(--line-gold);
  }
  .ig-lamp-flame {
    width: 5px;
    height: 8px;
    border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
    background: linear-gradient(180deg, var(--gold-soft), var(--gold));
    box-shadow: 0 0 10px 2px rgba(212, 175, 55, 0.5);
    animation: igFlicker 3.2s ease-in-out infinite;
  }
  .ig-lamp-bowl {
    width: 13px;
    height: 7px;
    margin-top: 1px;
    border: 1px solid var(--gold-deep);
    border-top: 0;
    border-radius: 0 0 8px 8px;
  }
  @keyframes igFlicker {
    0%,
    100% {
      opacity: 1;
      transform: scaleY(1);
    }
    50% {
      opacity: 0.78;
      transform: scaleY(0.88);
    }
  }

  .ig-hero-cta {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
  }
  .ig-credit {
    position: relative;
    max-width: 760px;
    margin: 26px auto 0;
    font-size: 11.5px;
    line-height: 1.6;
    color: var(--ink-soft);
    text-align: center;
  }
  .ig-credit a {
    color: var(--celest);
  }
  .ig-credit a:hover {
    color: var(--gold-deep);
  }

  /* ---------------- occasion cards ---------------- */
  .ig-cards {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(19rem, 1fr));
    gap: 18px;
    align-items: start;
  }
  .ig-card {
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 8px;
    padding: 24px 24px 6px;
    box-shadow: var(--shadow-soft);
    transition:
      border-color 0.15s,
      box-shadow 0.15s,
      transform 0.1s;
  }
  .ig-card:hover {
    border-color: var(--gold-deep);
    box-shadow: var(--shadow-card);
    transform: translateY(-1px);
  }
  .ig-card.is-large {
    grid-column: 1 / -1;
    border-color: var(--line-gold);
    background: linear-gradient(180deg, var(--paper), var(--ivory));
  }
  .ig-card-head {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 12px;
  }
  .ig-card-glyph {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 46px;
    height: 46px;
    flex: 0 0 auto;
    border-radius: 50%;
    background: var(--ivory-2);
    border: 1px solid var(--line-gold);
  }
  .ig-card-title {
    font-family: var(--serif);
    font-size: clamp(24px, 2.6vw, 30px);
    color: var(--byz-deep);
    line-height: 1.1;
    margin: 0 0 8px;
  }
  .ig-card-blurb {
    font-size: 15.5px;
    line-height: 1.62;
    color: var(--ink-soft);
    margin: 0;
  }

  /* expandable — native <details>, readable with JS off */
  .ig-more {
    margin-top: 4px;
  }
  .ig-more-sum {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 16px 0;
    cursor: pointer;
    list-style: none;
  }
  .ig-more-sum::-webkit-details-marker {
    display: none;
  }
  .ig-more-rule {
    flex: 1;
    height: 1px;
    background: var(--line-gold);
  }
  .ig-more-state {
    flex: 0 0 auto;
    font-size: 11px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--celest);
  }
  .ig-more[open] .ig-more-show,
  .ig-more:not([open]) .ig-more-hide {
    display: none;
  }
  .ig-chev {
    flex: 0 0 auto;
    transition: transform 0.15s ease;
  }
  .ig-more[open] .ig-chev {
    transform: rotate(180deg);
  }
  .ig-more-sum:hover .ig-more-state {
    color: var(--byz);
  }
  .ig-more-body {
    padding-bottom: 22px;
    animation: igOpen 0.34s cubic-bezier(0.2, 0.7, 0.3, 1) both;
  }
  @keyframes igOpen {
    from {
      opacity: 0;
      transform: translateY(-6px);
    }
  }

  /* ---------------- icon suggestion list ---------------- */
  .ig-icons {
    list-style: none;
    margin: 22px 0 0;
    padding: 0;
    display: grid;
    gap: 16px;
  }
  .is-large .ig-icons {
    grid-template-columns: repeat(auto-fill, minmax(17rem, 1fr));
  }
  .ig-icons--tight {
    margin-top: 16px;
  }
  .ig-icon {
    padding-left: 15px;
    border-left: 2px solid var(--line-gold);
  }
  .ig-icon-head {
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: 6px 10px;
    margin-bottom: 5px;
  }
  .ig-icon-name {
    font-family: var(--serif);
    font-weight: 600;
    font-size: 19px;
    color: var(--byz-deep);
    text-decoration: none;
    border-bottom: 1px solid var(--line-gold);
    transition:
      color 0.15s,
      border-color 0.15s;
  }
  a.ig-icon-name:hover {
    color: var(--gold-deep);
    border-bottom-color: var(--gold);
  }
  .ig-icon-name.is-plain {
    border-bottom: 0;
  }
  .ig-icon-why {
    font-size: 15px;
    line-height: 1.62;
    color: var(--ink);
    margin: 0;
  }
  .ig-icon-where {
    font-family: var(--serif);
    font-style: italic;
    font-size: 14px;
    color: var(--celest);
    margin: 5px 0 0;
  }

  /* tier pills */
  .ig-tier {
    flex: 0 0 auto;
    font-size: 9.5px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    padding: 3px 8px;
    border-radius: 999px;
    border: 1px solid;
    white-space: nowrap;
  }
  .ig-tier.is-traditional {
    color: var(--gold-deep);
    border-color: var(--line-gold);
    background: rgba(212, 175, 55, 0.09);
  }
  .ig-tier.is-regional {
    color: var(--byz-green);
    border-color: rgba(31, 94, 84, 0.32);
    background: rgba(31, 94, 84, 0.07);
  }
  .ig-tier.is-modern {
    color: var(--celest);
    border-color: var(--line);
    background: rgba(35, 76, 122, 0.05);
  }

  /* ---------------- vocation buttons ---------------- */
  .ig-vocs {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(15rem, 1fr));
    gap: 10px;
    margin-top: 22px;
  }
  .ig-voc {
    display: block;
    padding: 12px 14px;
    background: #fff;
    border: 1px solid var(--line);
    border-radius: 6px;
    text-decoration: none;
    transition:
      border-color 0.15s,
      box-shadow 0.15s,
      transform 0.1s;
  }
  .ig-voc:hover {
    border-color: var(--gold-deep);
    box-shadow: var(--shadow-card);
    transform: translateY(-1px);
  }
  .ig-voc-label {
    display: block;
    font-family: var(--serif);
    font-weight: 600;
    font-size: 17px;
    color: var(--byz-deep);
  }
  .ig-voc-note {
    display: block;
    font-size: 13px;
    line-height: 1.5;
    color: var(--ink-soft);
    margin-top: 2px;
  }

  /* ---------------- pulled-aside note ---------------- */
  .ig-note {
    position: relative;
    margin-top: 24px;
    padding: 22px 24px;
    background: var(--ivory-2);
    border: 1px solid var(--line-gold);
    border-radius: 6px;
  }
  .ig-note-title {
    font-family: var(--serif);
    font-weight: 600;
    font-size: 19px;
    color: var(--byz-deep);
    margin: 0 0 7px;
  }
  .ig-note-body {
    font-size: 15px;
    line-height: 1.64;
    color: var(--ink);
    margin: 0;
  }

  /* ---------------- facts ---------------- */
  .ig-facts {
    margin: 24px 0 0;
    display: grid;
    gap: 14px;
  }
  .is-large .ig-facts {
    grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
  }
  .ig-fact {
    border-top: 1px solid var(--line);
    padding-top: 12px;
  }
  .ig-fact dt {
    font-family: var(--text);
    font-weight: 500;
    font-size: 11px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--gold-deep);
    margin-bottom: 5px;
  }
  .ig-fact dd {
    margin: 0;
    font-size: 14.5px;
    line-height: 1.6;
    color: var(--ink-soft);
  }

  /* ---------------- tiers panel ---------------- */
  .ig-tiers {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(17rem, 1fr));
    gap: 18px;
  }
  .ig-tier-card {
    padding: 26px 24px;
    border-radius: 8px;
    border: 1px solid rgba(212, 175, 55, 0.26);
    background: rgba(255, 255, 255, 0.04);
  }
  .ig-tier-card.is-traditional {
    border-color: rgba(212, 175, 55, 0.55);
    background: rgba(212, 175, 55, 0.07);
  }
  .ig-tier-star {
    display: flex;
    align-items: center;
    gap: 7px;
    margin-bottom: 12px;
  }
  .ig-tier-star .dm {
    width: 6px;
    height: 6px;
    background: var(--gold);
    transform: rotate(45deg);
  }
  .ig-tier-label {
    font-family: var(--serif);
    font-weight: 600;
    font-size: 25px;
    color: var(--gold-soft);
    line-height: 1.1;
  }
  .ig-tier-lede {
    font-family: var(--serif);
    font-style: italic;
    font-size: 16px;
    color: var(--celest-soft);
    margin: 6px 0 14px;
  }
  .ig-tier-body {
    font-size: 15px;
    line-height: 1.65;
    color: var(--ivory-3);
    margin: 0 0 16px;
  }
  .ig-tier-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 9px;
  }
  .ig-tier-list li {
    position: relative;
    padding-left: 16px;
    font-size: 14px;
    line-height: 1.55;
    color: var(--ivory-2);
  }
  .ig-tier-list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 7px;
    width: 5px;
    height: 5px;
    background: var(--gold);
    transform: rotate(45deg);
  }

  /* ---------------- who are you buying for ---------------- */
  .ig-who-tabs {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 8px;
    margin-bottom: 28px;
  }
  .ig-who-tab {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    padding: 8px 15px;
    font-family: var(--text);
    font-size: 14.5px;
    color: var(--byz);
    background: #fff;
    border: 1px solid var(--line);
    border-radius: 999px;
    cursor: pointer;
    transition: all 0.15s;
  }
  .ig-who-tab:hover {
    border-color: var(--gold);
    color: var(--gold-deep);
  }
  .ig-who-tab[aria-expanded="true"] {
    background: var(--byz);
    border-color: var(--byz);
    color: var(--ivory);
  }
  .ig-who-tab[aria-expanded="true"] :global(svg) {
    stroke: var(--gold-soft);
  }
  .ig-who-glyph {
    display: flex;
    flex: 0 0 auto;
  }
  .ig-who-panels {
    display: grid;
    gap: 18px;
  }
  .ig-who-panel {
    background: var(--paper);
    border: 1px solid var(--line-gold);
    border-radius: 8px;
    padding: 26px clamp(20px, 3vw, 30px);
    animation: igOpen 0.3s cubic-bezier(0.2, 0.7, 0.3, 1) both;
  }
  .ig-who-title {
    font-family: var(--serif);
    font-size: 27px;
    color: var(--byz-deep);
    margin: 0 0 6px;
  }
  .ig-who-intro {
    font-family: var(--serif);
    font-style: italic;
    font-size: 17px;
    line-height: 1.5;
    color: var(--celest);
    margin: 0;
  }
  .ig-who-panel .ig-icons {
    grid-template-columns: repeat(auto-fill, minmax(16rem, 1fr));
  }

  /* ---------------- tips ---------------- */
  .ig-tips {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(16.5rem, 1fr));
    gap: 16px;
  }
  .ig-tip {
    position: relative;
    padding: 20px 22px 20px 24px;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 6px;
    transition:
      border-color 0.15s,
      box-shadow 0.15s;
  }
  .ig-tip:hover {
    border-color: var(--gold-deep);
    box-shadow: var(--shadow-soft);
  }
  .ig-tip-dm {
    position: absolute;
    left: 9px;
    top: 27px;
    width: 5px;
    height: 5px;
    background: var(--gold);
    transform: rotate(45deg);
  }
  .ig-tip-title {
    font-family: var(--serif);
    font-weight: 600;
    font-size: 19px;
    color: var(--byz-deep);
    line-height: 1.22;
    margin: 0 0 7px;
  }
  .ig-tip-body {
    font-size: 14.5px;
    line-height: 1.62;
    color: var(--ink-soft);
    margin: 0;
  }

  /* ---------------- related ---------------- */
  .ig-related {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(17rem, 1fr));
    gap: 16px;
  }
  .ig-rel {
    display: flex;
    align-items: flex-start;
    gap: 14px;
    padding: 22px;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 8px;
    text-decoration: none;
    box-shadow: var(--shadow-soft);
    transition:
      border-color 0.15s,
      box-shadow 0.15s,
      transform 0.1s;
  }
  .ig-rel:hover {
    border-color: var(--gold-deep);
    box-shadow: var(--shadow-card);
    transform: translateY(-1px);
  }
  .ig-rel-glyph {
    display: flex;
    align-items: center;
    justify-content: center;
    flex: 0 0 auto;
    width: 42px;
    height: 42px;
    border-radius: 50%;
    background: var(--ivory-2);
    border: 1px solid var(--line-gold);
  }
  .ig-rel-label {
    display: block;
    font-family: var(--serif);
    font-weight: 600;
    font-size: 20px;
    color: var(--byz-deep);
    line-height: 1.2;
  }
  .ig-rel:hover .ig-rel-label {
    color: var(--gold-deep);
  }
  .ig-rel-blurb {
    display: block;
    font-size: 14px;
    line-height: 1.55;
    color: var(--ink-soft);
    margin-top: 5px;
  }

  /* ---------------- responsive ---------------- */
  @media (max-width: 820px) {
    .ig-card {
      padding: 20px 20px 4px;
    }
    .is-large .ig-icons,
    .is-large .ig-facts {
      grid-template-columns: 1fr;
    }
    .ig-frame-img {
      width: 48px;
      height: 60px;
    }
    .is-center .ig-frame-img {
      width: 64px;
      height: 80px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ig-more-body,
    .ig-who-panel {
      animation: none;
    }
    .ig-lamp-flame {
      animation: none;
    }
    .ig-card:hover,
    .ig-voc:hover,
    .ig-rel:hover,
    .ig-corner-link:hover .ig-frame {
      transform: none;
    }
  }
</style>
